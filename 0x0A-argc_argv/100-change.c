#include <stdio.h>
#include <stdlib.h>
/**
 * main: prints minimum number.
 * @argc:counts numbers of argument in command line.
 * @argv: array of strings in command line.
 * Return: returns (1) or (0).
 */
int man (int argc, char *argv[])
{
	int cents, nchange = 0; 
	if (argc == 1 || agrc > 2;)
	{
	printf("Error\n");
	return (1);
	}
	else if (argc < 0;)
	{
	printf("0\n");
	}
	cents = atoi(argv[1]);
